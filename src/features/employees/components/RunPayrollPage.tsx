import { useState } from "react";import { useNavigate } from "react-router-dom";import { toast } from "sonner";import { PageHeader } from "@/components/common/PageHeader";import { PageContainer } from "@/components/layout/PageContainer";import { Button } from "@/components/ui/button";import { Card,CardContent } from "@/components/ui/card";import { Input } from "@/components/ui/input";import { Label } from "@/components/ui/label";import { Select,SelectContent,SelectItem,SelectTrigger,SelectValue } from "@/components/ui/select";import { useListEmployeesQuery,useListAdvancesQuery,useListBonusesQuery,useRunPayrollMutation } from "../employeesApi";
export function RunPayrollPage(){const navigate=useNavigate();const[employeeId,setEmployeeId]=useState("");const now=new Date();const[month,setMonth]=useState(now.getMonth()+1);const[year,setYear]=useState(now.getFullYear());const employees=useListEmployeesQuery();const advances=useListAdvancesQuery(employeeId,{skip:!employeeId});const bonuses=useListBonusesQuery(employeeId,{skip:!employeeId});const[run,state]=useRunPayrollMutation();const employee=employees.data?.data.find((e)=>e.id===employeeId);const periodBonuses=(bonuses.data?.data??[]).filter((b)=>{const d=new Date(b.bonusDate);return d.getMonth()+1===month&&d.getFullYear()===year;});return <PageContainer><PageHeader title="Run Payroll" description="Payroll is run for one employee at a time; the backend does not support department batches."/><Card><CardContent className="space-y-4 pt-6"><div><Label>Employee</Label><Select value={employeeId} onValueChange={setEmployeeId}><SelectTrigger><SelectValue placeholder="Select employee"/></SelectTrigger><SelectContent>{(employees.data?.data??[]).filter((e)=>e.isActive).map((e)=><SelectItem key={e.id} value={e.id}>{e.fullName}</SelectItem>)}</SelectContent></Select></div><div className="grid grid-cols-2 gap-4"><div><Label htmlFor="payroll-month">Period month</Label><Input id="payroll-month" type="number" min={1} max={12} value={month} onChange={(e)=>setMonth(Number(e.target.value))}/></div><div><Label htmlFor="payroll-year">Period year</Label><Input id="payroll-year" type="number" min={2000} value={year} onChange={(e)=>setYear(Number(e.target.value))}/></div></div>{employee?<div className="rounded border p-4 text-sm"><p>Known inputs before backend processing:</p><p>Base salary: {employee.baseSalary}</p><p>Bonuses in period: {periodBonuses.map((b)=>b.amount).join(", ")||"None"}</p><p>Outstanding advances: {(advances.data?.data??[]).filter((a)=>a.recoveryStatus!=="fully_recovered").map((a)=>`${a.amount} (${a.amountRecovered} recovered)`).join(", ")||"None"}</p><p className="text-muted-foreground">The authoritative deductions and net payable are calculated by the backend using oldest advances first.</p></div>:null}<Button disabled={!employeeId} isLoading={state.isLoading} onClick={async()=>{try{const result=await run({employeeId,periodMonth:month,periodYear:year}).unwrap();toast.success("Payroll run completed");navigate(`/payroll/runs/${result.data.id}`);}catch(error){const e=error as {status?:number};if(e.status===409)toast.error(`A salary run for ${employee?.fullName??"this employee"} for ${month}/${year} already exists`);else toast.error("Payroll could not be run");}}}>Run Payroll</Button></CardContent></Card></PageContainer>}
