import{useState}from"react";import{useParams}from"react-router-dom";import{toast}from"sonner";import{DataTable,type DataTableColumn}from"@/components/common/DataTable";import{EmptyState}from"@/components/common/EmptyState";import{ErrorState}from"@/components/common/ErrorState";import{PageHeader}from"@/components/common/PageHeader";import{PageSkeleton}from"@/components/common/Skeletons";import{PageContainer}from"@/components/layout/PageContainer";import{Button}from"@/components/ui/button";import{Dialog,DialogContent,DialogFooter,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{getApiErrorMessage}from"@/lib/api-error";import{useCreateFuelLogMutation,useGetVehicleQuery,useListFuelLogsQuery}from"../vehiclesApi";import type{CreateFuelLogRequest,VehicleFuelLog,VehiclePaymentMethod}from"../types";
/* eslint-disable react-hooks/immutability -- running total is scoped to one synchronous table render */
const money=new Intl.NumberFormat("en-PK",{style:"currency",currency:"PKR"});export function VehicleFuelLogsPage(){const{id=""}=useParams();const vehicle=useGetVehicleQuery(id,{skip:!id});const query=useListFuelLogsQuery(id,{skip:!id});const[open,setOpen]=useState(false);const[create,state]=useCreateFuelLogMutation();let running=0;const columns:DataTableColumn<VehicleFuelLog>[]=[{id:"date",header:"Fuel date",cell:(r)=>String(r.fuelDate).slice(0,10)},{id:"liters",header:"Liters",cell:(r)=>r.liters,align:"right"},{id:"rate",header:"Rate/liter",cell:(r)=>money.format(Number(r.ratePerLiter)),align:"right"},{id:"total",header:"Total",cell:(r)=>money.format(Number(r.totalAmount)),align:"right"},{id:"odometer",header:"Odometer",cell:(r)=>r.odometerReading??"—",align:"right"},{id:"method",header:"Payment",cell:(r)=>r.paymentMethod},{id:"running",header:"Running total",cell:(r)=>{running+=Number(r.totalAmount);return money.format(running)},align:"right"}];if(query.isLoading||vehicle.isLoading)return <PageSkeleton rows={6}/>;if(query.isError)return <PageContainer><ErrorState title="Fuel logs could not be loaded" onRetry={()=>void query.refetch()}/></PageContainer>;return <PageContainer><PageHeader title={`${vehicle.data?.data.registrationNumber??"Vehicle"} Fuel Logs`} description={vehicle.data?.data.vehicleType} actions={<Button onClick={()=>setOpen(true)}>Add Fuel Log</Button>}/><DataTable columns={columns} data={query.data?.data??[]} getRowId={(r)=>r.id} emptyContent={<EmptyState title="No fuel logs found"/>}/><p className="text-sm text-muted-foreground">Each fuel log automatically posts an Operating Expense under this vehicle’s department. Do not enter it again in Expenses.</p><FuelDialog open={open} loading={state.isLoading} onClose={()=>setOpen(false)} onSubmit={async(body)=>{try{await create({vehicleId:id,body}).unwrap();toast.success("Fuel log added");setOpen(false);}catch(e){toast.error(getApiErrorMessage(e));}}}/></PageContainer>}
function FuelDialog({open,loading,onClose,onSubmit}:{open:boolean;loading:boolean;onClose:()=>void;onSubmit:(body:CreateFuelLogRequest)=>Promise<void>}){const[date,setDate]=useState(new Date().toISOString().slice(0,10));const[liters,setLiters]=useState("");const[rate,setRate]=useState("");const[odo,setOdo]=useState("");const[method,setMethod]=useState<VehiclePaymentMethod>("cash");const[notes,setNotes]=useState("");const preview=Number(liters)*Number(rate);return <Dialog open={open} onOpenChange={(n)=>{if(!n)onClose();}}><DialogContent><DialogHeader><DialogTitle>Add Fuel Log</DialogTitle></DialogHeader><form className="space-y-4" onSubmit={(e)=>{e.preventDefault();if(Number(liters)>0&&Number(rate)>0)void onSubmit({fuelDate:date,liters:Number(liters),ratePerLiter:Number(rate),odometerReading:odo?Number(odo):undefined,paymentMethod:method,notes:notes||undefined});}}><div><Label htmlFor="fuel-date">Fuel date</Label><Input id="fuel-date" type="date" value={date} onChange={(e)=>setDate(e.target.value)}/></div><div><Label htmlFor="fuel-liters">Liters</Label><Input id="fuel-liters" type="number" min="0.01" value={liters} onChange={(e)=>setLiters(e.target.value)}/></div><div><Label htmlFor="fuel-rate">Rate per liter</Label><Input id="fuel-rate" type="number" min="0.01" value={rate} onChange={(e)=>setRate(e.target.value)}/></div><p>Preview total: {money.format(Number.isFinite(preview)?preview:0)}. The backend stores the authoritative total.</p><div><Label htmlFor="fuel-odo">Odometer reading</Label><Input id="fuel-odo" type="number" value={odo} onChange={(e)=>setOdo(e.target.value)}/></div><div><Label>Payment method</Label><Select value={method} onValueChange={(v)=>setMethod(v as VehiclePaymentMethod)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="cash">Cash</SelectItem><SelectItem value="bank">Bank</SelectItem></SelectContent></Select></div><div><Label htmlFor="fuel-notes">Notes</Label><Input id="fuel-notes" value={notes} onChange={(e)=>setNotes(e.target.value)}/></div><DialogFooter><Button type="button" variant="outline" onClick={onClose}>Cancel</Button><Button type="submit" isLoading={loading}>Save</Button></DialogFooter></form></DialogContent></Dialog>}
