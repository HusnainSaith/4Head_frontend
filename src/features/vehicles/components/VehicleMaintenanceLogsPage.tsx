import{useState}from"react";import{useParams}from"react-router-dom";import{toast}from"sonner";import{DataTable,type DataTableColumn}from"@/components/common/DataTable";import{EmptyState}from"@/components/common/EmptyState";import{ErrorState}from"@/components/common/ErrorState";import{PageHeader}from"@/components/common/PageHeader";import{PageSkeleton}from"@/components/common/Skeletons";import{PageContainer}from"@/components/layout/PageContainer";import{Button}from"@/components/ui/button";import{Dialog,DialogContent,DialogFooter,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{Input}from"@/components/ui/input";import{Label}from"@/components/ui/label";import{Select,SelectContent,SelectItem,SelectTrigger,SelectValue}from"@/components/ui/select";import{getApiErrorMessage}from"@/lib/api-error";import{useCreateMaintenanceLogMutation,useGetVehicleQuery,useListMaintenanceLogsQuery}from"../vehiclesApi";import type{CreateMaintenanceLogRequest,VehicleMaintenanceLog,VehiclePaymentMethod}from"../types";
/* eslint-disable react-hooks/immutability -- running total is scoped to one synchronous table render */
const money=new Intl.NumberFormat("en-PK",{style:"currency",currency:"PKR"});export function VehicleMaintenanceLogsPage(){const{id=""}=useParams();const vehicle=useGetVehicleQuery(id,{skip:!id});const query=useListMaintenanceLogsQuery(id,{skip:!id});const[open,setOpen]=useState(false);const[create,state]=useCreateMaintenanceLogMutation();let running=0;const columns:DataTableColumn<VehicleMaintenanceLog>[]=[{id:"date",header:"Maintenance date",cell:(r)=>String(r.maintenanceDate).slice(0,10)},{id:"type",header:"Type",cell:(r)=>r.maintenanceType},{id:"description",header:"Description",cell:(r)=>r.description??"—"},{id:"cost",header:"Cost",cell:(r)=>money.format(Number(r.cost)),align:"right"},{id:"vendor",header:"Vendor",cell:(r)=>r.vendorName??"—"},{id:"method",header:"Payment",cell:(r)=>r.paymentMethod},{id:"running",header:"Running total",cell:(r)=>{running+=Number(r.cost);return money.format(running)},align:"right"}];if(query.isLoading||vehicle.isLoading)return <PageSkeleton rows={6}/>;if(query.isError)return <PageContainer><ErrorState title="Maintenance logs could not be loaded" onRetry={()=>void query.refetch()}/></PageContainer>;return <PageContainer><PageHeader title={`${vehicle.data?.data.registrationNumber??"Vehicle"} Maintenance Logs`} description={vehicle.data?.data.vehicleType} actions={<Button onClick={()=>setOpen(true)}>Add Maintenance Log</Button>}/><DataTable columns={columns} data={query.data?.data??[]} getRowId={(r)=>r.id} emptyContent={<EmptyState title="No maintenance logs found"/>}/><p className="text-sm text-muted-foreground">Each maintenance log automatically posts an Operating Expense under this vehicle’s department. Do not enter it again in Expenses.</p><MaintenanceDialog open={open} loading={state.isLoading} onClose={()=>setOpen(false)} onSubmit={async(body)=>{try{await create({vehicleId:id,body}).unwrap();toast.success("Maintenance log added");setOpen(false);}catch(e){toast.error(getApiErrorMessage(e));}}}/></PageContainer>}
function MaintenanceDialog({open,loading,onClose,onSubmit}:{open:boolean;loading:boolean;onClose:()=>void;onSubmit:(body:CreateMaintenanceLogRequest)=>Promise<void>}){const[date,setDate]=useState(new Date().toISOString().slice(0,10));const[type,setType]=useState("");const[description,setDescription]=useState("");const[cost,setCost]=useState("");const[vendor,setVendor]=useState("");const[method,setMethod]=useState<VehiclePaymentMethod>("cash");return <Dialog open={open} onOpenChange={(n)=>{if(!n)onClose();}}><DialogContent><DialogHeader><DialogTitle>Add Maintenance Log</DialogTitle></DialogHeader><form className="space-y-4" onSubmit={(e)=>{e.preventDefault();if(type&&Number(cost)>0)void onSubmit({maintenanceDate:date,maintenanceType:type,description:description||undefined,cost:Number(cost),vendorName:vendor||undefined,paymentMethod:method});}}><div><Label htmlFor="maintenance-date">Maintenance date</Label><Input id="maintenance-date" type="date" value={date} onChange={(e)=>setDate(e.target.value)}/></div><div><Label htmlFor="maintenance-type">Maintenance type</Label><Input id="maintenance-type" value={type} onChange={(e)=>setType(e.target.value)}/></div><div><Label htmlFor="maintenance-description">Description</Label><Input id="maintenance-description" value={description} onChange={(e)=>setDescription(e.target.value)}/></div><div><Label htmlFor="maintenance-cost">Cost</Label><Input id="maintenance-cost" type="number" min="0.01" value={cost} onChange={(e)=>setCost(e.target.value)}/></div><div><Label htmlFor="maintenance-vendor">Vendor name</Label><Input id="maintenance-vendor" value={vendor} onChange={(e)=>setVendor(e.target.value)}/></div><div><Label>Payment method</Label><Select value={method} onValueChange={(v)=>setMethod(v as VehiclePaymentMethod)}><SelectTrigger><SelectValue/></SelectTrigger><SelectContent><SelectItem value="cash">Cash</SelectItem><SelectItem value="bank">Bank</SelectItem></SelectContent></Select></div><DialogFooter><Button type="button" variant="outline" onClick={onClose}>Cancel</Button><Button type="submit" isLoading={loading}>Save</Button></DialogFooter></form></DialogContent></Dialog>}
