/** Mirrors backend src/modules/roles/role.enum.ts exactly. */
export enum Role {
  OWNER = "owner",
  ACCOUNTANT = "accountant",
  DEPARTMENT_STAFF = "department_staff",
  EMPLOYEE = "employee",
}

/** Mirrors backend src/common/types/department-code.enum.ts exactly. */
export enum DepartmentCode {
  BROKERAGE = "brokerage",
  SUPPLY = "supply",
  WASTAGE = "wastage",
  FRESH_CHICKEN_SHOP = "fresh_chicken_shop",
}
