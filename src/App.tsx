/**
 * Historical default export kept as a thin re-export of `@/app/App` (the
 * feature-based root). Prefer importing from `@/app/App` directly. This file
 * exists only so tooling that imports `./App` keeps resolving.
 */
export { default } from "@/app/App";
