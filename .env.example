# Local development API. Production builds discard loopback values and use
# same-origin requests unless deployment supplies a non-loopback URL.
VITE_API_BASE_URL=http://localhost:3000

# The component style guide is available only in development.
VITE_ENABLE_DEV_PAGES=true
